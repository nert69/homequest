# Copy this file to .env and fill in your Supabase project's values
# (Settings → API in the Supabase dashboard) to enable syncing between
# devices. Leave .env absent/empty and the app still works fine —
# it just won't sync between phones.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
